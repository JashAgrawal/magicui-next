export * from './components/magic-ui';
// No longer exporting from nLib/handler as it has been removed/refactored.
// The API route src/app/api/generate-magic-ui/route.ts is the server-side entry point.
// Client-side components and hooks are exported via './components/magic-ui'.